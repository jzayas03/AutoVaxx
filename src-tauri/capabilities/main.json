{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The synthetic-only shell receives no Tauri core plugin permissions.",
  "windows": ["main"],
  "permissions": []
}
